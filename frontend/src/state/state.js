import { createStore } from "vuex";
export default createStore({
  state: {
    count: 0,
    is_loggedIn :false
  },

  //   mutations: {
  //     increment(state) {
  //       state.count++;
  //     },
  //   },
});
